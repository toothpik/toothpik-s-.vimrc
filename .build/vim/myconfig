#!/bin/bash
#  whack old config, re-config from scratch
cd ~/.build/vim/vim
rm -f src/auto/config.cache
date '+%Y-%b-%d %H:%M' >> ../configure.log
./configure --with-features=big --with-compiledby=toothpik@home 2>&1 | tee -a ../configure.log
dsh 60 >> ../configure.log
