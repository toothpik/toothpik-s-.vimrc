#!/bin/bash
#  battery life on steroids
clear
tsl=`tput lines`
tcols=`tput cols`
if test $tsl -gt 50; then
    b
    pfc=$(( ( (tcols - 75) / 2) - 2 ))
    cm1p1 | mio $pfc
    b
fi
hm=$(( ( (tcols - 75) / 2) - 1 ))
{
    b2
    hoirb
    b
    mb
    b
    tpr
    b
    tpk
} | mio $hm
b
