#!/bin/bash
#  battery life on steroids
clear
tsl=`tput lines`
if test $tsl -gt 40; then
    b
    pfc=$(( ( (`tput cols` - 75) / 2) - 2 ))
    cm1p1 | mio $pfc
    b
fi
hm=16
b2    | mio $hm
hoirb | mio $hm
b
mb    | mio $hm
b
