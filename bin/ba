#!/bin/bash
clear
tsl=`tput lines`
if test $tsl -gt 37; then
    b
    pfc=$(( ( (`tput cols` - 75) / 2) - 2 ))
    cm1p1 | mio $pfc
    b
fi
ma=8
b2    | mio $ma
hoirb | mio $ma
b
rb    | mio $ma
b
mb    | mio $ma
b
