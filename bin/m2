#!/bin/bash
clear
b
tsl=`tput lines`
if test $tsl -gt 37; then
    pfc=$(( ( (`tput cols` - 75) / 2) - 2 ))
    cm1p1 | mio $pfc
    b
fi
cy=`date '+%y'`
wm=`curpmo`
nm=$((wm + 1))
if test $nm -gt 12; then
    y=$((cy + 1))
    m=1
else
    y=$cy
    m=$nm
fi
m$y $m | mio 8
