#!/bin/bash
#  open text plan in gvim
if [ $# -eq 0 ]; then
    Y=`date '+%Y'`
else
    Y=$1
fi
g -c 'badd ~/txt/plan_2013' -c 'source ~/.vim/plan.vim' -c 'call GoLastX()' -c 'mark h' ~/txt/plan_${Y} 
