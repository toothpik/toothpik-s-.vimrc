#!/bin/bash
#  cat weather [moved over w possible header]
#tsl=`tput lines`
#if test $tsl -gt 50; then
#    hbh=$(( $tsl - `cm1p1 | wc -l` - 3 ))
#    clear
#    b
#    pfc=$(( ( (`tput cols` - 75) / 2) - 2 ))
#    cm1p1 | mio $pfc
#    b
#    ~/tcl/cw 2>&1 | head -$hbh | fixtoday | mio 8
#else
    c
    b
    ~/tcl/cw 2>&1 | hsf | fixtoday | mio 8
#fi
